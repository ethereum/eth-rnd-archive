## Phase0

- Use `.copy()` consistently (#5601)
- Add `saturating_sub` helper function (#5620)
- Add executable gossip validation functions for heze (#5605)

## Altair

- Use `.copy()` consistently (#5601)
- Use block's slot to validate max blobs in gossip (#5614)
- Add `saturating_sub` helper function (#5620)

## Bellatrix

- Use `.copy()` consistently (#5601)
- Convert `NewPayloadRequest` from dataclass to container (#5619)

## Capella

- Use `.copy()` consistently (#5601)
- Use `is_future_epoch` in BLS to execution change gossip (#5600)
- Fix fork comment for removed `process_historical_roots_update` (#5636)

## Deneb

- Use `.copy()` consistently (#5601)
- Convert `NewPayloadRequest` from dataclass to container (#5619)

## Electra

- Convert `NewPayloadRequest` from dataclass to container (#5619)

## Fulu

- Use `.copy()` consistently (#5601)
- Use block's slot to validate max blobs in gossip (#5614)
- Convert `NewPayloadRequest` from dataclass to container (#5619)

## Gloas

- Use `.copy()` consistently (#5601)
- Add missing dependent block checks to `on_inclusion_list` (#5599)
- Introduce `compute_shuffling_lookahead_start_slot` helper (#5602)
- Use block's slot to validate max blobs in gossip (#5614)
- Only serve canonical payload envelopes by range (#5608)
- Clarify when to broadcast payload attestation messages (#5612)
- Compute max `DataColumnSidecar` size based on blob schedule (#5613)
- Convert `NewPayloadRequest` from dataclass to container (#5619)
- Add weak subjectivity period table for gloas (#5629)
- Cache `compute_balance_weighted_selection` (#5631)
- Add `saturating_sub` helper function (#5620)
- Reject pre-Gloas slots in `get_ptc` (#5652)
- Add executable gossip validation functions for heze (#5605)

## Heze

- Use `.copy()` consistently (#5601)
- Add missing dependent block checks to `on_inclusion_list` (#5599)
- Introduce `compute_shuffling_lookahead_start_slot` helper (#5602)
- Add executable gossip validation functions for heze (#5605)

## EIP-8025

- Use `.copy()` consistently (#5601)
- Convert `NewPayloadRequest` from dataclass to container (#5619)

## EIP-8321

- Add executable gossip validation functions for heze (#5605)

## Testing

- Use `.copy()` consistently (#5601)
- Use `is_future_epoch` in BLS to execution change gossip (#5600)
- Add missing dependent block checks to `on_inclusion_list` (#5599)
- Introduce `compute_shuffling_lookahead_start_slot` helper (#5602)
- Add a test when a dependent block is the head (#5603)
- Use block's slot to validate max blobs in gossip (#5614)
- Compute max `DataColumnSidecar` size based on blob schedule (#5613)
- Extend existing tests to cover Heze (#5615)
- Convert `NewPayloadRequest` from dataclass to container (#5619)
- Resolve outstanding Gloas test TODOs (#5617)
- Clean up Makefile (#5628)
- Do not emit ssz-static deposits post-Fulu (#5640)
- Fix attestation step order in FCR test vectors (#5627)
- Fix fork epochs in proposer preferences tests (#5646)
- Test RANDAO checks for execution payload bids on the head's parent (#5645)
- Remove complicated/broken BTEC tests (#5654)
- Fix typo in comment (#5648)
- Reject pre-Gloas slots in `get_ptc` (#5652)
- Add executable gossip validation functions for heze (#5605)

## Dependencies

- Update dependency zensical to v0.0.59 (#5607)
- Update all dependencies (#5633)
- Update all dependencies (#5649)

## Other

- Replace AGENTS.md with simplified CLAUDE.md file (#5604)
- Preserve packaged tests for release upload retries (#5616)
- Bump version to v1.7.0-beta.1 (#5626)
- Use `make build` in the combine-coverages action (#5632)

